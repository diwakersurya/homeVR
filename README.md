# homeVR
this branch hosts the site.
a few changes from master branch
1. npm run bundle-- for creating bundle.js files
2. moved index.html to root folder and made changes to point to generated files.

